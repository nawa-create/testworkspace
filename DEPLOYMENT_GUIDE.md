# デプロイ手順書

このガイドでは、FAX受信データ検証システムを初めてデプロイする方法を**画像付きで詳しく**説明します。

## 📋 事前準備

- Googleアカウント
- スプレッドシートへのアクセス権限

## 🚀 ステップ1: Google Apps Scriptプロジェクトの作成

### 1-1. GASエディタを開く

1. ブラウザで https://script.google.com/ にアクセス
2. Googleアカウントでログイン
3. 左上の「新しいプロジェクト」ボタンをクリック

### 1-2. プロジェクト名を設定

1. 左上の「無題のプロジェクト」をクリック
2. 「FAX受信データ検証システム」と入力
3. 「名前を変更」をクリック

## 📝 ステップ2: コードをコピー

### 2-1. Code.gsの編集

1. デフォルトで`Code.gs`ファイルが開いています
2. 既存のコード（`function myFunction() { ... }`）を全て削除
3. このリポジトリの`Code.gs`の内容を**全て**コピー
4. GASエディタに貼り付け
5. **Ctrl+S**（または Cmd+S）で保存

### 2-2. Index.htmlの作成

1. 左側のファイル一覧で「**+**」ボタンをクリック
2. 「HTML」を選択
3. ファイル名に「**Index**」と入力（拡張子.htmlは自動追加されます）
4. このリポジトリの`Index.html`の内容を**全て**コピー
5. GASエディタに貼り付け
6. **Ctrl+S**（または Cmd+S）で保存

## ⚙️ ステップ3: スプレッドシートIDの確認

### 3-1. スプレッドシートIDの取得

スプレッドシートのURLから IDを取得します：

```
https://docs.google.com/spreadsheets/d/【ここがID】/edit#gid=0
```

例: `1sT_UQFSmWBuUza4H1Xp1nmgWYqS0WbvcuhjhSjtNvE8`

### 3-2. Code.gsに設定

`Code.gs`の2行目を確認：

```javascript
const SPREADSHEET_ID = '1sT_UQFSmWBuUza4H1Xp1nmgWYqS0WbvcuhjhSjtNvE8';
```

あなたのスプレッドシートIDに変更してください。

## 🚀 ステップ4: デプロイ（最重要！）

### 4-1. 新しいデプロイの作成

1. 右上の「**デプロイ**」ボタンをクリック
2. 「**新しいデプロイ**」を選択

### 4-2. ウェブアプリとして設定

1. 「種類の選択」（⚙️歯車アイコン）をクリック
2. 「**ウェブアプリ**」を選択

### 4-3. デプロイ設定（重要）

以下の設定を正確に行ってください：

| 項目 | 設定値 |
|------|--------|
| **説明** | 初回デプロイ（任意） |
| **次のユーザーとして実行** | **自分（あなたのメールアドレス）** |
| **アクセスできるユーザー** | **全員** |

### 4-4. デプロイの実行

1. 「**デプロイ**」ボタンをクリック

### 4-5. 権限の承認（初回のみ）

初回デプロイ時、権限の承認が必要です：

1. 「**アクセスを承認**」をクリック
2. Googleアカウントを選択
3. 警告画面が表示される場合:
   - 「**詳細**」をクリック
   - 「**（プロジェクト名）に移動**」をクリック
4. 権限の確認画面:
   - スプレッドシートの表示・編集
   - 外部サービスへの接続
5. 「**許可**」をクリック

### 4-6. ウェブアプリURLの取得

1. デプロイが完了すると、**ウェブアプリのURL**が表示されます
2. このURLをコピー（例: `https://script.google.com/macros/s/XXXXX/exec`）
3. 「**完了**」をクリック

## 🌐 ステップ5: アクセスと動作確認

### 5-1. Webアプリを開く

1. コピーしたURLを**新しいタブ**で開く
2. 「データを読み込んでいます...」と「**接続テスト**」ボタンが表示されます

### 5-2. 接続テスト

1. 「**接続テスト**」ボタンをクリック
2. ✅ **成功の場合**:
   - 「接続成功！」というアラートが表示
   - 自動的にデータ読み込みが開始
3. ❌ **失敗の場合**:
   - トラブルシューティングセクションを参照

### 5-3. データ確認

- 左側にFAX受信データの一覧が表示されます
- データをクリックすると、中央に詳細、右側に画像が表示されます

## 🔄 コード更新時の手順

コードを修正した場合は、**デプロイを更新**する必要があります：

### 更新手順

1. Code.gs または Index.html を編集
2. **Ctrl+S**（Cmd+S）で保存
3. 「**デプロイ**」→「**デプロイを管理**」
4. 既存のデプロイの**鉛筆アイコン**（編集）をクリック
5. 「**バージョン**」を「**新バージョン**」に変更
6. 「**デプロイ**」をクリック
7. ブラウザでWebアプリを開いて**強制リロード**（Ctrl+F5 または Cmd+Shift+R）

**重要**: URLは変わりません！

## ⚠️ よくある間違い

### ❌ 間違い1: テストボタンで開いている

GASエディタの右上にある「テスト」ボタン（▶️）で開いた場合、`google.script.run`が正しく動作しません。

**✅ 正解**: デプロイで発行された「ウェブアプリURL」を開く

### ❌ 間違い2: デプロイを更新していない

コードを変更したのに反映されない場合、デプロイを更新していない可能性があります。

**✅ 正解**: 「デプロイを管理」から「新バージョン」でデプロイ

### ❌ 間違い3: ブラウザキャッシュ

デプロイを更新したのに変わらない場合、ブラウザのキャッシュが原因です。

**✅ 正解**: 強制リロード（Ctrl+F5 または Cmd+Shift+R）

## 🐛 トラブルシューティング

問題が発生した場合は、`README.md`の「トラブルシューティング」セクションを参照してください。

特に「データを読み込んでいます」が表示され続ける場合は、デプロイ設定を確認してください。

## 📞 サポート

それでも解決しない場合は、以下の情報を添えてIssueを作成してください：

1. ブラウザのコンソール（F12）のログ
2. GASエディタの「実行ログ」の内容
3. 行った手順とエラーメッセージ
